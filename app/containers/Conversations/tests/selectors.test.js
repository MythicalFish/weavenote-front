// import { fromJS } from 'immutable';
// import { makeSelectConversationsDomain } from '../selectors';

// const selector = makeSelectConversationsDomain();

describe('makeSelectConversationsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
