// import { fromJS } from 'immutable';
// import { makeSelectProjectListItemDomain } from '../selectors';

// const selector = makeSelectProjectListItemDomain();

describe('makeSelectProjectListItemDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
