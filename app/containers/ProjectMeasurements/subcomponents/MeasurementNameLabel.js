import React, { PropTypes } from 'react';

const MeasurementNameLabel = (props) => <div />;

MeasurementNameLabel.propTypes = {
  fieldName: PropTypes.string,
  handleSubmit: PropTypes.func,
};

export default MeasurementNameLabel;
