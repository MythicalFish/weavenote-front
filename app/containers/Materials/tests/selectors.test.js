// import { fromJS } from 'immutable';
// import { makeSelectMaterialsDomain } from '../selectors';

// const selector = makeSelectMaterialsDomain();

describe('makeSelectMaterialsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
